using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityUI;

public class ObjectCollect : MonoBehaviour {

    private void Start(other)
    {
        var pickable = other.GetComponent<Pickable>();

        if (picked = true)
        {

        }
    }
}
